# Global working rules

Understand the task before editing.
For non-trivial tasks, inspect relevant files and write a short plan first.
Prefer the smallest safe change that fully solves the task.
Do not modify unrelated files.
After editing, run the most relevant checks available.
If verification cannot be completed, state exactly what was not verified.